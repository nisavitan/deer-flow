# Port implementation progress

Central status doc. Updated at every milestone. Upstream basis: `0950924`. Branch: `port/claude-code-architecture`.

User decisions in force (2026-08-01): TypeScript · install from fork/local only (no marketplace before parity + clean-env install) · provider-dependent skills = optional modules, disabled by default, capability detection · v1 summarization = native compaction + structured checkpoint summaries, delta documented, context-loss parity tests required.

| Milestone | Status | Commit | Tests (exit code) | Original components covered | Parity classification | New Claude-specific code | Known gaps | Next |
|---|---|---|---|---|---|---|---|---|
| Baseline | **done** | `dc7c7ab9` | Original anchor suite 532 passed (exit 0); extractor deterministic (byte-identical reruns) | n/a (extraction, no port code) | n/a | extract_vectors.py (55KB, throwaway tooling) | Live-model baseline of original blocked by no-API-key constraint (baseline/README.md, 5 recorded skips) | — |
| M1 foundation | **done** | `e2be20c2` | Headless smokes pass (PONG, hook log, deep-run stub) | none (scaffolding) | n/a | plugin manifest, TS toolchain, stub skill/workflow/hook (all Claude-specific) | stubs replaced in M3/M6 | — |
| M2 state library | **done** | `98c4f34e` | 164 vitest tests, 115 baseline vectors consumed, 0 discrepancies (exit 0) | thread_state.py reducers (delegations, skill_context, goal, promoted, artifacts, todos), subagent caps clamps, goal counters/breaker, run identity | **exact** (vector-equal): all reducers+clamps+counters; **approximate**: atomic-io rev CAS (replaces LangGraph channel versioning); **omitted**: merge_sandbox, merge_viewed_images (consumers designed out) | atomic-io.ts, paths.ts (state-file discipline — no original equivalent, replaces checkpointer plumbing) | sessions.json/summary.json channels land in M8/M10 | — |
| M3 lead prompt | **done** | `b909c403` | 0 unexplained diffs vs 3 golden renders; drift detection proven; 197 tests total (exit 0) | prompt.py apply_prompt_template + all section builders, factory.py todo prompt | **exact**: section order/text (golden-equal); **approximate**: 18 whitelisted substitutions (tool names, paths, delegation mechanism phrasing); **omitted**: 5 declared uncovered branches (substitutions.ts) | SKILL.md composition layer, deep-run routing section | memory turn-context injection deferred to M9 | — |
| M4 skills | **done** | `cf8bec25` | 23/23 skills carried (16 regular + 7 optional gated); headless body-load smoke passed | skills/public/** (all 23 packages) | **exact** (bodies verbatim): 14; **approximate** (paths/tool-names adapted): 9; **omitted**: none (provider-dependent = optional modules, not dropped) | capability-check gates (7), skills-optional README, install-skill.sh root-marker adaptation | root tests/skills still target original tree (parity test in M14) | — |
| M5 tools/sandbox | **done** | (this commit) | 93 new env-scrub tests; 290 total (exit 0); allow+deny smokes pass | sandbox/env_policy.py (lists verbatim), sandbox/tools.py (mapping contract), present_file_tool.py (contract doc) | **exact**: scrub name lists/patterns; **approximate**: enforcement shape (command-refusal at PreToolUse vs child-env scrub — platform owns the spawn; stricter direction, sanctioned by S24); **approximate**: present_files → outputs/ contract (delivery enforcement weakened to prompt policy until M13 receipt check) | env-guard.ts hook, permissions-preset.json (23 deny rules), sandbox-contract.md | Bash literal-match deny rules evadable by obfuscation (closing = M7 audit classifier); delivery enforcement restored in M13 | adversarial-fixture gate |
| M6 subagents/deep-run | **done** | (this commit) | 640 total tests (exit 0; M6-scoped tsc clean); smokes: caps 3-of-4 with verbatim limit note; gates A5-A7 passed | executor.py _aexecute (dispatch/timeout/failure mapping), task_tool.py (plumbing), subagent_limit_middleware.py (caps), status_contract.py (60 golden renders byte-exact), builtins/general_purpose+bash (prompts verbatim mod. enumerated substitutions) | **exact**: result formatting, caps arithmetic, limit note, status vocabulary, agent prompts; **approximate**: timeout (enforced, but cancellation one-way), failure signal (schema-forced vs error_fallback marker); **omitted**: step events SSE (progress tree+ledger cover), polling_timed_out status (agent() blocks); **self-reported-only**: turn/token/loop_capped (no dispatcher observation — DISCREPANCIES #2) | deep-run.js workflow (sandbox-constrained twin pattern + workflow-sync.test.ts), ledger-io.ts, args coercion | ledger persisted by lead session post-workflow (PostToolUse fires at async launch — structurally impossible in-hook; DISCREPANCIES #7); token_collector.py row pending | M7 wires turn-context + ledger persistence instruction |
| M7 middleware hooks | **done** | `7a74b062`+`e8587502` | 159 new tests; 1258 total (exit 0); live smokes verbatim; gate A8 passed live (warn@3, hard-block@5, denials=2) | loop_detection (byte-exact canonicalizer, 117 md5 literals match), tool_result_meta (38+4 whole-object), read_before_write, dynamic_context date reminder, ledger persistence | **exact**: loop algorithm+messages, tool-meta taxonomy, hash marks; **approximate**: enforcement point (after_model→PreToolUse, fires sooner never later), marks in state file vs messages; **deferred**: tool_progress (disabled-by-default in original, argued in-file); **omitted**: output-budget externalization (platform-native, soft 20k warn kept) | hook-runtime plumbing, tool-adapter translation layer, python-json canonicalizer | skill_context_entry stamping pending (skill-context lane); deerflow.json threshold override unwired (one-line) | — |
| M8 context/summarization | **done** | `ca30b26d` | 93 new tests (911 then-total, exit 0 scoped; later full-green) | summarization_middleware wrapper+authority contract (verbatim, drift-tested), context_compaction manual flow, summary_text channel | **exact**: wrapper, LastValue merge, authority contract; **approximate**: summary content (native compaction + deterministic digest), carrier (additionalContext vs SystemMessage); **omitted**: trigger/keep tunability (platform-owned), TAG_NOSTREAM | PreCompact digest hook, context-loss measurement harness, digest CLI | memory-flush window was blocked-on-M9 (now closable); context-loss fixtures authored in M14 | — |
| M9 memory | **done** | `2ca23e90` | 217 new tests; npm check exit 0 (1189) | DeerMem v2 layout (exact sharding), 4 extraction prompt YAMLs (byte-identical), write-gate (fail-closed matrix), injection budget 2000+500 | **exact**: prompts, layout, gate rules, injection budget; **approximate**: debounce→batch-on-next-turn, middleware capture→Stop-hook queue; **omitted**: FTS5 retrieval, locks/revisions/journal (single-writer), staleness/consolidation cycles (creation clamp only) | queue.ts, gate/store CLIs keeping LLM proposals behind deterministic code | staleness/consolidation selection layers deferred | — |
| M10 checkpoints/resume | **done** | `186974cc` (amended) | 78 new tests; full 1200 green, full tsc exit 0; live orphan smoke | worker.py orphan reconciliation (behavioral), staleness binding, resume report | **exact**: receipt backfill invariants, terminal guard; **approximate**: leases/heartbeat→2h expiry constant, interrupted-vs-error naming (declared); **n/a**: per-superstep rollback | session-recover hook, status CLI, git-head fail-soft | summary_text key coupling noted (shared constant later) | — |
| M11 errors/goal loop | **done** | `07b437ea` | 86 new tests (1187 total green) | runtime/goal.py evaluator rubric (verbatim), continuation flow, stand-down taxonomy, terminal-response backstop | **exact**: rubric text, caps 8/2, blocker enum, gates outside model control; **degraded (declared)**: evaluator independence (judge=author, mitigated), hidden continuation→visible block reason, thread-unchanged→rev CAS | Stop-hook persist-before-block design, goal CLI | — | — |
| M12 permissions | **done** | `a9f7f9b5` | evidenced by gate A1 (recorded) | authz two-layer model (collapse declared) | **approximate**: layer-1→permission rules+agent tools, layer-2→PreToolUse hooks; **omitted (intentional)**: RBAC identity/roles (single-user CLI) | sandbox-pairing guidance | — | — |
| M13 artifacts | **done** | (this commit) | 96 new tests; 1354 total (exit 0); live smoke: block-then-pass + full receipt | workspace_changes/{types,scanner,diff,recorder,api}.py (limits byte-for-byte), run-worker delivery verdict (verbatim error text), present_file_tool enforcement | **exact**: scan limits, sensitive/binary metadata-only rules, receipt fields, delivery error text; **relocated (declared)**: run-failure→turn-block, present_files call→final-message inference (permissive direction stated); **omitted**: unified diff content (no UI consumer); **degraded**: run-level→turn-level snapshot granularity | turn-snapshot + delivery-gate hooks, workspace-changes.json capped history | stop_hook_active suppression records-but-not-enforces on the suppressed turn | — |
| M14 parity suite | **done** | `3f0c68c5`+`16b00709` (+A9 `b0df7ce1`) | npm run parity 629/629 exit 0; npm run check 1393/1393 exit 0 (post-fixes) | full matrix accounting + evidence map | **Five-way (evidence-based, over 158)**: exact 18 (11.4%) · approximate 43 (27.2%) · omitted 23 (14.6%) · blocked 2 (1.3%) · **unverified 72 (45.6%)**; evidenced equivalence **61/158 = 38.6%** (disposition view: 84.2%) | parity-report.md, context-loss harness+fixtures+CLI, hooks.jsonl O3 log | compaction-recall leg headless-untriggerable (platform finding; joint session) | joint 15 checks |
| M15 packaging | **done** | `a1bbe2ed` | — | — | — | INSTALL.md, ATTRIBUTION.md, v0.9.0-rc1 | clean-env install = joint check #1 | — |
| M16 upstream sync | **done** | `a1bbe2ed` | dry run executed vs 4 real new upstream commits | — | — | sync playbook | 1 backlog item (audit command-substitution fix → env-guard) | — |

Additional rules (user directive 2026-08-01, second round):
- Every milestone row reports three extra fields: original components covered · parity classification (exact / approximate / omitted / blocked) · new Claude-specific code added — so the final parity percentage cannot be inflated by many tests on small components.
- **Adversarial-fixture gate before real-project use**: M5/M6 (and the hook chain from M7) must pass the deliberate failure fixture at `ports/claude-code/parity/fixtures/adversarial/` (crashing tool, hanging tool, subagent returning schema-invalid output, repository containing sensitive files) BEFORE being pointed at any real project. No early wiring to real kickoff code.
- M14 behavioral comparison uses fixtures + structured, comparable outputs — never verbatim-model-output equality.

Rules in force: separate commit per milestone; traceability-matrix.md updated with every replaced component; nothing marked "ported" without code + passing parity test; parity outcomes labeled exact / approximate / intentionally omitted / blocked; independent verifier after every significant milestone.

## Closure status (2026-08-01, pre-joint-validation)

User's 7 closure requirements: (1) fix commits documented ✅ (`3f0c68c5`, `b0df7ce1`, `16b00709`); (2) matrix without misleading planned ✅ (78 genuine planned, 44 platform-native, 9 excluded); (3) hooks.jsonl proven live ✅ (full-lifecycle 15-line run + A9 trail + subprocess deny proof); (4) memory flush: unit+subprocess+enqueue ✅, live compaction leg NOT headless-triggerable (documented platform finding; moved to joint session as its first candidate check); (5) npm run parity post-fixes ✅ 629/629; (6) five-way recomputation from corrected matrix ✅ incl. Unverified; (7) independent verifier — running, results recorded below/next commit.

Honest bottom line: the port's **core engine spine is implemented with strong evidence** (vectors byte-exact where claimed, live gates A1–A9); the **long tail is explicitly unverified (45.6%)**, mostly rows with no port code (61) — enumerated in parity-report §8. The joint validation session decides readiness classification.
